# Содержимое google-credentials.json одной строкой (JSON)
# В Streamlit Cloud: Settings → Secrets → вставить содержимое этого файла
GOOGLE_CREDENTIALS = '{"type": "service_account", "project_id": "...", "private_key": "...", ...}'
